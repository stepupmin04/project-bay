export interface Product {
    id?: number;
    firstName?: string;
    lastName?: string;
    email?: string;
    address?: string;
}

export interface Title {
    code?: string;
    name?: string;
}